import 'package:flutter/material.dart';

class MulitSelection extends StatelessWidget {
  const MulitSelection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.green,
    );
  }
}